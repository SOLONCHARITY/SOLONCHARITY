const CoinbaseLogo = ({ width, height, className }) => {
  return <img src="/coinbase-logo.svg" alt="Coinbase Wallet Logo" width={width} height={height} className={className} />
}

export default CoinbaseLogo

