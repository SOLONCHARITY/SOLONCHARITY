const MetaMaskLogo = ({ width, height, className }) => {
  return <img src="/metamask-logo.svg" alt="MetaMask Logo" width={width} height={height} className={className} />
}

export default MetaMaskLogo

